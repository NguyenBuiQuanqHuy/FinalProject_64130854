<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:background="#fffcdb"
    android:orientation="vertical"
    tools:context=".ModeActivity">

    <!-- CardView cho nút chế độ ôn tập -->
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:cardElevation="15dp"
        android:cardBackgroundColor="@color/yellow"
        app:cardCornerRadius="16dp">

        <Button
            android:id="@+id/btn_practice_mode"
            android:layout_width="300dp"
            android:layout_height="60dp"
            android:backgroundTint="@android:color/transparent"
            android:text="Exam Mode"
            android:textColor="@color/black"
            android:textSize="30dp"
            android:textStyle="bold" />
    </androidx.cardview.widget.CardView>

    <!-- CardView cho nút chế độ làm bài -->
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:cardElevation="15dp"
        android:cardBackgroundColor="@color/yellow"
        app:cardCornerRadius="16dp">

        <Button
            android:id="@+id/btn_exam_mode"
            android:layout_width="300dp"
            android:layout_height="60dp"
            android:backgroundTint="@android:color/transparent"
            android:text="Practice Mode"
            android:textColor="@color/black"
            android:textSize="30dp"
            android:textStyle="bold" />
    </androidx.cardview.widget.CardView>

</LinearLayout>
